---
layout: default
title: AP Computer Science A
---

<h2>
<a name="{{ page.title }}" class="anchor" href="#{{ page.title }}"><span class="octicon octicon-link"></span></a>{{ page.title }}</h2>

<p><a href="about.html">About {{ page.title }}</a> | <a href="allposts.html">All posts in {{ page.title }}</a></p>

{% for post in site.posts %}
<section>
{{ post.url }} {{ post.title }} {{ post.content }} {{ post.categories }} {{ site.categories }}
{% if post.categories == "apcsa" %}
hello<p>
    <a href="{{ post.url }}">
      <h3>{{ post.title }}</h3>
    </a>
	{{ post.content }}
</p>
{% endif %}
</section>
{% endfor %}
